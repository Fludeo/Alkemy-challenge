import AbstractResolver from "./AbstractResolver";
import { IDIContainer } from "../types";
export declare type Factory = (container: IDIContainer) => any;
/**
 * FactoryResolver - allows to use custom function to build dependency
 */
export default class FactoryResolver<T extends Factory> extends AbstractResolver<ReturnType<T>> {
    private readonly factory;
    constructor(factory: T);
    resolve: (container: IDIContainer) => ReturnType<T>;
}
